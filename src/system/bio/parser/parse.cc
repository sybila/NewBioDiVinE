// Generated by Bisonc++ V2.09.03 on Thu, 22 May 2014 17:35:27 +0200

// $insert class.ih
#include "Parser.ih"

// The FIRST element of SR arrays shown below uses `d_type', defining the
// state's type, and `d_lastIdx' containing the last element's index. If
// d_lastIdx contains the REQ_TOKEN bitflag (see below) then the state needs
// a token: if in this state d_token__ is _UNDETERMINED_, nextToken() will be
// called

// The LAST element of SR arrays uses `d_token' containing the last retrieved
// token to speed up the (linear) seach.  Except for the first element of SR
// arrays, the field `d_action' is used to determine what to do next. If
// positive, it represents the next state (used with SHIFT); if zero, it
// indicates `ACCEPT', if negative, -d_action represents the number of the
// rule to reduce to.

// `lookup()' tries to find d_token__ in the current SR array. If it fails, and
// there is no default reduction UNEXPECTED_TOKEN__ is thrown, which is then
// caught by the error-recovery function.

// The error-recovery function will pop elements off the stack until a state
// having bit flag ERR_ITEM is found. This state has a transition on _error_
// which is applied. In this _error_ state, while the current token is not a
// proper continuation, new tokens are obtained by nextToken(). If such a
// token is found, error recovery is successful and the token is
// handled according to the error state's SR table and parsing continues.
// During error recovery semantic actions are ignored.

// A state flagged with the DEF_RED flag will perform a default
// reduction if no other continuations are available for the current token.

// The ACCEPT STATE never shows a default reduction: when it is reached the
// parser returns ACCEPT(). During the grammar
// analysis phase a default reduction may have been defined, but it is
// removed during the state-definition phase.

// So:
//      s_x[] = 
//      {
//                  [_field_1_]         [_field_2_]
//
// First element:   {state-type,        idx of last element},
// Other elements:  {required token,    action to perform},
//                                      ( < 0: reduce, 
//                                          0: ACCEPT,
//                                        > 0: next state)
// Last element:    {set to d_token__,    action to perform}
//      }

// When the --thread-safe option is specified, all static data are defined as
// const. If --thread-safe is not provided, the state-tables are not defined
// as const, since the lookup() function below will modify them


namespace // anonymous
{
    char const author[] = "Frank B. Brokken (f.b.brokken@rug.nl)";

    enum 
    {
        STACK_EXPANSION = 5     // size to expand the state-stack with when
                                // full
    };

    enum ReservedTokens
    {
        PARSE_ACCEPT     = 0,   // `ACCEPT' TRANSITION
        _UNDETERMINED_   = -2,
        _EOF_            = -1,
        _error_          = 256
    };
    enum StateType       // modify statetype/data.cc when this enum changes
    {
        NORMAL,
        ERR_ITEM,
        REQ_TOKEN,
        ERR_REQ,    // ERR_ITEM | REQ_TOKEN
        DEF_RED,    // state having default reduction
        ERR_DEF,    // ERR_ITEM | DEF_RED
        REQ_DEF,    // REQ_TOKEN | DEF_RED
        ERR_REQ_DEF // ERR_ITEM | REQ_TOKEN | DEF_RED
    };    
    struct PI__     // Production Info
    {
        size_t d_nonTerm; // identification number of this production's
                            // non-terminal 
        size_t d_size;    // number of elements in this production 
    };

    struct SR__     // Shift Reduce info, see its description above
    {
        union
        {
            int _field_1_;      // initializer, allowing initializations 
                                // of the SR s_[] arrays
            int d_type;
            int d_token;
        };
        union
        {
            int _field_2_;

            int d_lastIdx;          // if negative, the state uses SHIFT
            int d_action;           // may be negative (reduce), 
                                    // postive (shift), or 0 (accept)
            size_t d_errorState;    // used with Error states
        };
    };

    // $insert staticdata
    
// Productions Info Records:
PI__ const s_productionInfo[] = 
{
     {0, 0}, // not used: reduction values are negative
     {271, 2}, // 1: start ('\x0a') ->  line '\x0a'
     {271, 3}, // 2: start ('\x0a') ->  start line '\x0a'
     {272, 0}, // 3: line ->  <empty>
     {272, 2}, // 4: line (EQ_LINE) ->  EQ_LINE eq_defs
     {272, 2}, // 5: line (PARAM_LINE) ->  PARAM_LINE param_defs
     {272, 2}, // 6: line (VAR_LINE) ->  VAR_LINE var_defs
     {272, 2}, // 7: line (THRES_LINE) ->  THRES_LINE thres_defs
     {272, 2}, // 8: line (CONST_LINE) ->  CONST_LINE const_defs
     {272, 2}, // 9: line (INIT_LINE) ->  INIT_LINE init_defs
     {272, 1}, // 10: line (BA_LINE) ->  BA_LINE
     {272, 2}, // 11: line (VAR_POINTS_LINE) ->  VAR_POINTS_LINE var_points_defs
     {279, 1}, // 12: var_points_defs ->  var_points_def
     {279, 3}, // 13: var_points_defs (';') ->  var_points_defs ';' var_points_def
     {280, 3}, // 14: var_points_def (':') ->  var_points_name ':' var_points_values
     {281, 1}, // 15: var_points_name (NAME) ->  NAME
     {282, 3}, // 16: var_points_values (',') ->  var_points_value ',' NUMBER
     {283, 1}, // 17: var_points_value (NUMBER) ->  NUMBER
     {278, 1}, // 18: init_defs ->  init_def
     {278, 3}, // 19: init_defs (';') ->  init_defs ';' init_def
     {284, 3}, // 20: init_def (':') ->  init_name ':' init_interval
     {285, 1}, // 21: init_name (NAME) ->  NAME
     {286, 3}, // 22: init_interval (',') ->  init_value ',' NUMBER
     {287, 1}, // 23: init_value (NUMBER) ->  NUMBER
     {277, 1}, // 24: const_defs ->  const_def
     {277, 3}, // 25: const_defs (';') ->  const_defs ';' const_def
     {288, 3}, // 26: const_def (',') ->  const_name ',' const_value
     {289, 1}, // 27: const_name (NAME) ->  NAME
     {290, 1}, // 28: const_value (NUMBER) ->  NUMBER
     {290, 2}, // 29: const_value ('-') ->  '-' NUMBER
     {276, 3}, // 30: thres_defs (':') ->  thres_name ':' thres_values
     {291, 1}, // 31: thres_name (NAME) ->  NAME
     {292, 1}, // 32: thres_values (NUMBER) ->  NUMBER
     {292, 3}, // 33: thres_values (',') ->  thres_values ',' NUMBER
     {275, 1}, // 34: var_defs (NAME) ->  NAME
     {275, 3}, // 35: var_defs (',') ->  var_defs ',' NAME
     {274, 1}, // 36: param_defs ->  param_def
     {274, 3}, // 37: param_defs (';') ->  param_defs ';' param_def
     {293, 3}, // 38: param_def (',') ->  param_name ',' param_interval
     {295, 3}, // 39: param_interval (',') ->  param_value ',' NUMBER
     {295, 4}, // 40: param_interval (',') ->  param_value ',' '-' NUMBER
     {294, 1}, // 41: param_name (NAME) ->  NAME
     {296, 1}, // 42: param_value (NUMBER) ->  NUMBER
     {296, 2}, // 43: param_value ('-') ->  '-' NUMBER
     {273, 3}, // 44: eq_defs ('=') ->  eq_var '=' expr
     {297, 1}, // 45: eq_var (NAME) ->  NAME
     {299, 1}, // 46: ap (NUMBER) ->  NUMBER
     {299, 1}, // 47: ap (NAME) ->  NAME
     {299, 1}, // 48: ap (RAMP) ->  RAMP
     {299, 1}, // 49: ap (SIGM) ->  SIGM
     {299, 1}, // 50: ap (STEP) ->  STEP
     {299, 1}, // 51: ap (HILL) ->  HILL
     {299, 2}, // 52: ap ('-') ->  '-' NUMBER
     {299, 2}, // 53: ap ('-') ->  '-' NAME
     {299, 2}, // 54: ap ('-') ->  '-' RAMP
     {299, 2}, // 55: ap ('-') ->  '-' SIGM
     {299, 2}, // 56: ap ('-') ->  '-' STEP
     {299, 2}, // 57: ap ('-') ->  '-' HILL
     {298, 1}, // 58: expr ->  summation
     {298, 1}, // 59: expr ->  ap
     {300, 1}, // 60: summation ->  summant
     {300, 3}, // 61: summation ('+') ->  summation '+' summant
     {300, 3}, // 62: summation ('-') ->  summation '-' summant
     {301, 1}, // 63: summant ->  multiplication
     {301, 1}, // 64: summant ->  parenthesis
     {302, 1}, // 65: multiplication ->  multiple
     {302, 3}, // 66: multiplication ('*') ->  multiplication '*' multiple
     {304, 1}, // 67: multiple ->  ap
     {304, 1}, // 68: multiple ->  parenthesis
     {303, 3}, // 69: parenthesis ('(') ->  '(' expr ')'
     {305, 1}, // 70: start_$ ->  start
};

// State info and SR__ transitions for each state.


SR__ s_0[] =
{
    { { REQ_DEF}, { 11} },                   
    { {     271}, {  1} }, // start          
    { {     272}, {  2} }, // line           
    { {     259}, {  3} }, // EQ_LINE        
    { {     258}, {  4} }, // PARAM_LINE     
    { {     257}, {  5} }, // VAR_LINE       
    { {     260}, {  6} }, // THRES_LINE     
    { {     261}, {  7} }, // CONST_LINE     
    { {     262}, {  8} }, // INIT_LINE      
    { {     263}, {  9} }, // BA_LINE        
    { {     264}, { 10} }, // VAR_POINTS_LINE
    { {       0}, { -3} },                   
};

SR__ s_1[] =
{
    { { REQ_DEF}, {           11} },                   
    { {     272}, {           11} }, // line           
    { {     259}, {            3} }, // EQ_LINE        
    { {     258}, {            4} }, // PARAM_LINE     
    { {     257}, {            5} }, // VAR_LINE       
    { {     260}, {            6} }, // THRES_LINE     
    { {     261}, {            7} }, // CONST_LINE     
    { {     262}, {            8} }, // INIT_LINE      
    { {     263}, {            9} }, // BA_LINE        
    { {     264}, {           10} }, // VAR_POINTS_LINE
    { {   _EOF_}, { PARSE_ACCEPT} },                   
    { {       0}, {           -3} },                   
};

SR__ s_2[] =
{
    { { REQ_TOKEN}, {  2} },          
    { {        10}, { 12} }, // '\x0a'
    { {         0}, {  0} },          
};

SR__ s_3[] =
{
    { { REQ_TOKEN}, {  4} },           
    { {       273}, { 13} }, // eq_defs
    { {       297}, { 14} }, // eq_var 
    { {       265}, { 15} }, // NAME   
    { {         0}, {  0} },           
};

SR__ s_4[] =
{
    { { REQ_TOKEN}, {  5} },              
    { {       274}, { 16} }, // param_defs
    { {       293}, { 17} }, // param_def 
    { {       294}, { 18} }, // param_name
    { {       265}, { 19} }, // NAME      
    { {         0}, {  0} },              
};

SR__ s_5[] =
{
    { { REQ_TOKEN}, {  3} },            
    { {       275}, { 20} }, // var_defs
    { {       265}, { 21} }, // NAME    
    { {         0}, {  0} },            
};

SR__ s_6[] =
{
    { { REQ_TOKEN}, {  4} },              
    { {       276}, { 22} }, // thres_defs
    { {       291}, { 23} }, // thres_name
    { {       265}, { 24} }, // NAME      
    { {         0}, {  0} },              
};

SR__ s_7[] =
{
    { { REQ_TOKEN}, {  5} },              
    { {       277}, { 25} }, // const_defs
    { {       288}, { 26} }, // const_def 
    { {       289}, { 27} }, // const_name
    { {       265}, { 28} }, // NAME      
    { {         0}, {  0} },              
};

SR__ s_8[] =
{
    { { REQ_TOKEN}, {  5} },             
    { {       278}, { 29} }, // init_defs
    { {       284}, { 30} }, // init_def 
    { {       285}, { 31} }, // init_name
    { {       265}, { 32} }, // NAME     
    { {         0}, {  0} },             
};

SR__ s_9[] =
{
    { { DEF_RED}, {   1} }, 
    { {       0}, { -10} }, 
};

SR__ s_10[] =
{
    { { REQ_TOKEN}, {  5} },                   
    { {       279}, { 33} }, // var_points_defs
    { {       280}, { 34} }, // var_points_def 
    { {       281}, { 35} }, // var_points_name
    { {       265}, { 36} }, // NAME           
    { {         0}, {  0} },                   
};

SR__ s_11[] =
{
    { { REQ_TOKEN}, {  2} },          
    { {        10}, { 37} }, // '\x0a'
    { {         0}, {  0} },          
};

SR__ s_12[] =
{
    { { DEF_RED}, {  1} }, 
    { {       0}, { -1} }, 
};

SR__ s_13[] =
{
    { { DEF_RED}, {  1} }, 
    { {       0}, { -4} }, 
};

SR__ s_14[] =
{
    { { REQ_TOKEN}, {  2} },       
    { {        61}, { 38} }, // '='
    { {         0}, {  0} },       
};

SR__ s_15[] =
{
    { { DEF_RED}, {   1} }, 
    { {       0}, { -45} }, 
};

SR__ s_16[] =
{
    { { REQ_DEF}, {  2} },       
    { {      59}, { 39} }, // ';'
    { {       0}, { -5} },       
};

SR__ s_17[] =
{
    { { DEF_RED}, {   1} }, 
    { {       0}, { -36} }, 
};

SR__ s_18[] =
{
    { { REQ_TOKEN}, {  2} },       
    { {        44}, { 40} }, // ','
    { {         0}, {  0} },       
};

SR__ s_19[] =
{
    { { DEF_RED}, {   1} }, 
    { {       0}, { -41} }, 
};

SR__ s_20[] =
{
    { { REQ_DEF}, {  2} },       
    { {      44}, { 41} }, // ','
    { {       0}, { -6} },       
};

SR__ s_21[] =
{
    { { DEF_RED}, {   1} }, 
    { {       0}, { -34} }, 
};

SR__ s_22[] =
{
    { { DEF_RED}, {  1} }, 
    { {       0}, { -7} }, 
};

SR__ s_23[] =
{
    { { REQ_TOKEN}, {  2} },       
    { {        58}, { 42} }, // ':'
    { {         0}, {  0} },       
};

SR__ s_24[] =
{
    { { DEF_RED}, {   1} }, 
    { {       0}, { -31} }, 
};

SR__ s_25[] =
{
    { { REQ_DEF}, {  2} },       
    { {      59}, { 43} }, // ';'
    { {       0}, { -8} },       
};

SR__ s_26[] =
{
    { { DEF_RED}, {   1} }, 
    { {       0}, { -24} }, 
};

SR__ s_27[] =
{
    { { REQ_TOKEN}, {  2} },       
    { {        44}, { 44} }, // ','
    { {         0}, {  0} },       
};

SR__ s_28[] =
{
    { { DEF_RED}, {   1} }, 
    { {       0}, { -27} }, 
};

SR__ s_29[] =
{
    { { REQ_DEF}, {  2} },       
    { {      59}, { 45} }, // ';'
    { {       0}, { -9} },       
};

SR__ s_30[] =
{
    { { DEF_RED}, {   1} }, 
    { {       0}, { -18} }, 
};

SR__ s_31[] =
{
    { { REQ_TOKEN}, {  2} },       
    { {        58}, { 46} }, // ':'
    { {         0}, {  0} },       
};

SR__ s_32[] =
{
    { { DEF_RED}, {   1} }, 
    { {       0}, { -21} }, 
};

SR__ s_33[] =
{
    { { REQ_DEF}, {   2} },       
    { {      59}, {  47} }, // ';'
    { {       0}, { -11} },       
};

SR__ s_34[] =
{
    { { DEF_RED}, {   1} }, 
    { {       0}, { -12} }, 
};

SR__ s_35[] =
{
    { { REQ_TOKEN}, {  2} },       
    { {        58}, { 48} }, // ':'
    { {         0}, {  0} },       
};

SR__ s_36[] =
{
    { { DEF_RED}, {   1} }, 
    { {       0}, { -15} }, 
};

SR__ s_37[] =
{
    { { DEF_RED}, {  1} }, 
    { {       0}, { -2} }, 
};

SR__ s_38[] =
{
    { { REQ_TOKEN}, { 16} },                  
    { {       298}, { 49} }, // expr          
    { {       300}, { 50} }, // summation     
    { {       299}, { 51} }, // ap            
    { {       301}, { 52} }, // summant       
    { {       266}, { 53} }, // NUMBER        
    { {       265}, { 54} }, // NAME          
    { {       267}, { 55} }, // RAMP          
    { {       268}, { 56} }, // SIGM          
    { {       269}, { 57} }, // STEP          
    { {       270}, { 58} }, // HILL          
    { {        45}, { 59} }, // '-'           
    { {       302}, { 60} }, // multiplication
    { {       303}, { 61} }, // parenthesis   
    { {       304}, { 62} }, // multiple      
    { {        40}, { 63} }, // '('           
    { {         0}, {  0} },                  
};

SR__ s_39[] =
{
    { { REQ_TOKEN}, {  4} },              
    { {       293}, { 64} }, // param_def 
    { {       294}, { 18} }, // param_name
    { {       265}, { 19} }, // NAME      
    { {         0}, {  0} },              
};

SR__ s_40[] =
{
    { { REQ_TOKEN}, {  5} },                  
    { {       295}, { 65} }, // param_interval
    { {       296}, { 66} }, // param_value   
    { {       266}, { 67} }, // NUMBER        
    { {        45}, { 68} }, // '-'           
    { {         0}, {  0} },                  
};

SR__ s_41[] =
{
    { { REQ_TOKEN}, {  2} },        
    { {       265}, { 69} }, // NAME
    { {         0}, {  0} },        
};

SR__ s_42[] =
{
    { { REQ_TOKEN}, {  3} },                
    { {       292}, { 70} }, // thres_values
    { {       266}, { 71} }, // NUMBER      
    { {         0}, {  0} },                
};

SR__ s_43[] =
{
    { { REQ_TOKEN}, {  4} },              
    { {       288}, { 72} }, // const_def 
    { {       289}, { 27} }, // const_name
    { {       265}, { 28} }, // NAME      
    { {         0}, {  0} },              
};

SR__ s_44[] =
{
    { { REQ_TOKEN}, {  4} },               
    { {       290}, { 73} }, // const_value
    { {       266}, { 74} }, // NUMBER     
    { {        45}, { 75} }, // '-'        
    { {         0}, {  0} },               
};

SR__ s_45[] =
{
    { { REQ_TOKEN}, {  4} },             
    { {       284}, { 76} }, // init_def 
    { {       285}, { 31} }, // init_name
    { {       265}, { 32} }, // NAME     
    { {         0}, {  0} },             
};

SR__ s_46[] =
{
    { { REQ_TOKEN}, {  4} },                 
    { {       286}, { 77} }, // init_interval
    { {       287}, { 78} }, // init_value   
    { {       266}, { 79} }, // NUMBER       
    { {         0}, {  0} },                 
};

SR__ s_47[] =
{
    { { REQ_TOKEN}, {  4} },                   
    { {       280}, { 80} }, // var_points_def 
    { {       281}, { 35} }, // var_points_name
    { {       265}, { 36} }, // NAME           
    { {         0}, {  0} },                   
};

SR__ s_48[] =
{
    { { REQ_TOKEN}, {  4} },                     
    { {       282}, { 81} }, // var_points_values
    { {       283}, { 82} }, // var_points_value 
    { {       266}, { 83} }, // NUMBER           
    { {         0}, {  0} },                     
};

SR__ s_49[] =
{
    { { DEF_RED}, {   1} }, 
    { {       0}, { -44} }, 
};

SR__ s_50[] =
{
    { { REQ_DEF}, {   3} },       
    { {      43}, {  84} }, // '+'
    { {      45}, {  85} }, // '-'
    { {       0}, { -58} },       
};

SR__ s_51[] =
{
    { { REQ_DEF}, {   3} },          
    { {      10}, { -59} }, // '\x0a'
    { {      41}, { -59} }, // ')'   
    { {       0}, { -67} },          
};

SR__ s_52[] =
{
    { { DEF_RED}, {   1} }, 
    { {       0}, { -60} }, 
};

SR__ s_53[] =
{
    { { DEF_RED}, {   1} }, 
    { {       0}, { -46} }, 
};

SR__ s_54[] =
{
    { { DEF_RED}, {   1} }, 
    { {       0}, { -47} }, 
};

SR__ s_55[] =
{
    { { DEF_RED}, {   1} }, 
    { {       0}, { -48} }, 
};

SR__ s_56[] =
{
    { { DEF_RED}, {   1} }, 
    { {       0}, { -49} }, 
};

SR__ s_57[] =
{
    { { DEF_RED}, {   1} }, 
    { {       0}, { -50} }, 
};

SR__ s_58[] =
{
    { { DEF_RED}, {   1} }, 
    { {       0}, { -51} }, 
};

SR__ s_59[] =
{
    { { REQ_TOKEN}, {  7} },          
    { {       266}, { 86} }, // NUMBER
    { {       265}, { 87} }, // NAME  
    { {       267}, { 88} }, // RAMP  
    { {       268}, { 89} }, // SIGM  
    { {       269}, { 90} }, // STEP  
    { {       270}, { 91} }, // HILL  
    { {         0}, {  0} },          
};

SR__ s_60[] =
{
    { { REQ_DEF}, {   2} },       
    { {      42}, {  92} }, // '*'
    { {       0}, { -63} },       
};

SR__ s_61[] =
{
    { { REQ_DEF}, {   2} },       
    { {      42}, { -68} }, // '*'
    { {       0}, { -64} },       
};

SR__ s_62[] =
{
    { { DEF_RED}, {   1} }, 
    { {       0}, { -65} }, 
};

SR__ s_63[] =
{
    { { REQ_TOKEN}, { 16} },                  
    { {       298}, { 93} }, // expr          
    { {       300}, { 50} }, // summation     
    { {       299}, { 51} }, // ap            
    { {       301}, { 52} }, // summant       
    { {       266}, { 53} }, // NUMBER        
    { {       265}, { 54} }, // NAME          
    { {       267}, { 55} }, // RAMP          
    { {       268}, { 56} }, // SIGM          
    { {       269}, { 57} }, // STEP          
    { {       270}, { 58} }, // HILL          
    { {        45}, { 59} }, // '-'           
    { {       302}, { 60} }, // multiplication
    { {       303}, { 61} }, // parenthesis   
    { {       304}, { 62} }, // multiple      
    { {        40}, { 63} }, // '('           
    { {         0}, {  0} },                  
};

SR__ s_64[] =
{
    { { DEF_RED}, {   1} }, 
    { {       0}, { -37} }, 
};

SR__ s_65[] =
{
    { { DEF_RED}, {   1} }, 
    { {       0}, { -38} }, 
};

SR__ s_66[] =
{
    { { REQ_TOKEN}, {  2} },       
    { {        44}, { 94} }, // ','
    { {         0}, {  0} },       
};

SR__ s_67[] =
{
    { { DEF_RED}, {   1} }, 
    { {       0}, { -42} }, 
};

SR__ s_68[] =
{
    { { REQ_TOKEN}, {  2} },          
    { {       266}, { 95} }, // NUMBER
    { {         0}, {  0} },          
};

SR__ s_69[] =
{
    { { DEF_RED}, {   1} }, 
    { {       0}, { -35} }, 
};

SR__ s_70[] =
{
    { { REQ_DEF}, {   2} },       
    { {      44}, {  96} }, // ','
    { {       0}, { -30} },       
};

SR__ s_71[] =
{
    { { DEF_RED}, {   1} }, 
    { {       0}, { -32} }, 
};

SR__ s_72[] =
{
    { { DEF_RED}, {   1} }, 
    { {       0}, { -25} }, 
};

SR__ s_73[] =
{
    { { DEF_RED}, {   1} }, 
    { {       0}, { -26} }, 
};

SR__ s_74[] =
{
    { { DEF_RED}, {   1} }, 
    { {       0}, { -28} }, 
};

SR__ s_75[] =
{
    { { REQ_TOKEN}, {  2} },          
    { {       266}, { 97} }, // NUMBER
    { {         0}, {  0} },          
};

SR__ s_76[] =
{
    { { DEF_RED}, {   1} }, 
    { {       0}, { -19} }, 
};

SR__ s_77[] =
{
    { { DEF_RED}, {   1} }, 
    { {       0}, { -20} }, 
};

SR__ s_78[] =
{
    { { REQ_TOKEN}, {  2} },       
    { {        44}, { 98} }, // ','
    { {         0}, {  0} },       
};

SR__ s_79[] =
{
    { { DEF_RED}, {   1} }, 
    { {       0}, { -23} }, 
};

SR__ s_80[] =
{
    { { DEF_RED}, {   1} }, 
    { {       0}, { -13} }, 
};

SR__ s_81[] =
{
    { { DEF_RED}, {   1} }, 
    { {       0}, { -14} }, 
};

SR__ s_82[] =
{
    { { REQ_TOKEN}, {  2} },       
    { {        44}, { 99} }, // ','
    { {         0}, {  0} },       
};

SR__ s_83[] =
{
    { { DEF_RED}, {   1} }, 
    { {       0}, { -17} }, 
};

SR__ s_84[] =
{
    { { REQ_TOKEN}, {  14} },                  
    { {       301}, { 100} }, // summant       
    { {       302}, {  60} }, // multiplication
    { {       303}, {  61} }, // parenthesis   
    { {       304}, {  62} }, // multiple      
    { {        40}, {  63} }, // '('           
    { {       299}, { 101} }, // ap            
    { {       266}, {  53} }, // NUMBER        
    { {       265}, {  54} }, // NAME          
    { {       267}, {  55} }, // RAMP          
    { {       268}, {  56} }, // SIGM          
    { {       269}, {  57} }, // STEP          
    { {       270}, {  58} }, // HILL          
    { {        45}, {  59} }, // '-'           
    { {         0}, {   0} },                  
};

SR__ s_85[] =
{
    { { REQ_TOKEN}, {  14} },                  
    { {       301}, { 102} }, // summant       
    { {       302}, {  60} }, // multiplication
    { {       303}, {  61} }, // parenthesis   
    { {       304}, {  62} }, // multiple      
    { {        40}, {  63} }, // '('           
    { {       299}, { 101} }, // ap            
    { {       266}, {  53} }, // NUMBER        
    { {       265}, {  54} }, // NAME          
    { {       267}, {  55} }, // RAMP          
    { {       268}, {  56} }, // SIGM          
    { {       269}, {  57} }, // STEP          
    { {       270}, {  58} }, // HILL          
    { {        45}, {  59} }, // '-'           
    { {         0}, {   0} },                  
};

SR__ s_86[] =
{
    { { DEF_RED}, {   1} }, 
    { {       0}, { -52} }, 
};

SR__ s_87[] =
{
    { { DEF_RED}, {   1} }, 
    { {       0}, { -53} }, 
};

SR__ s_88[] =
{
    { { DEF_RED}, {   1} }, 
    { {       0}, { -54} }, 
};

SR__ s_89[] =
{
    { { DEF_RED}, {   1} }, 
    { {       0}, { -55} }, 
};

SR__ s_90[] =
{
    { { DEF_RED}, {   1} }, 
    { {       0}, { -56} }, 
};

SR__ s_91[] =
{
    { { DEF_RED}, {   1} }, 
    { {       0}, { -57} }, 
};

SR__ s_92[] =
{
    { { REQ_TOKEN}, {  12} },               
    { {       304}, { 103} }, // multiple   
    { {       299}, { 101} }, // ap         
    { {       303}, { 104} }, // parenthesis
    { {       266}, {  53} }, // NUMBER     
    { {       265}, {  54} }, // NAME       
    { {       267}, {  55} }, // RAMP       
    { {       268}, {  56} }, // SIGM       
    { {       269}, {  57} }, // STEP       
    { {       270}, {  58} }, // HILL       
    { {        45}, {  59} }, // '-'        
    { {        40}, {  63} }, // '('        
    { {         0}, {   0} },               
};

SR__ s_93[] =
{
    { { REQ_TOKEN}, {   2} },       
    { {        41}, { 105} }, // ')'
    { {         0}, {   0} },       
};

SR__ s_94[] =
{
    { { REQ_TOKEN}, {   3} },          
    { {       266}, { 106} }, // NUMBER
    { {        45}, { 107} }, // '-'   
    { {         0}, {   0} },          
};

SR__ s_95[] =
{
    { { DEF_RED}, {   1} }, 
    { {       0}, { -43} }, 
};

SR__ s_96[] =
{
    { { REQ_TOKEN}, {   2} },          
    { {       266}, { 108} }, // NUMBER
    { {         0}, {   0} },          
};

SR__ s_97[] =
{
    { { DEF_RED}, {   1} }, 
    { {       0}, { -29} }, 
};

SR__ s_98[] =
{
    { { REQ_TOKEN}, {   2} },          
    { {       266}, { 109} }, // NUMBER
    { {         0}, {   0} },          
};

SR__ s_99[] =
{
    { { REQ_TOKEN}, {   2} },          
    { {       266}, { 110} }, // NUMBER
    { {         0}, {   0} },          
};

SR__ s_100[] =
{
    { { DEF_RED}, {   1} }, 
    { {       0}, { -61} }, 
};

SR__ s_101[] =
{
    { { DEF_RED}, {   1} }, 
    { {       0}, { -67} }, 
};

SR__ s_102[] =
{
    { { DEF_RED}, {   1} }, 
    { {       0}, { -62} }, 
};

SR__ s_103[] =
{
    { { DEF_RED}, {   1} }, 
    { {       0}, { -66} }, 
};

SR__ s_104[] =
{
    { { DEF_RED}, {   1} }, 
    { {       0}, { -68} }, 
};

SR__ s_105[] =
{
    { { DEF_RED}, {   1} }, 
    { {       0}, { -69} }, 
};

SR__ s_106[] =
{
    { { DEF_RED}, {   1} }, 
    { {       0}, { -39} }, 
};

SR__ s_107[] =
{
    { { REQ_TOKEN}, {   2} },          
    { {       266}, { 111} }, // NUMBER
    { {         0}, {   0} },          
};

SR__ s_108[] =
{
    { { DEF_RED}, {   1} }, 
    { {       0}, { -33} }, 
};

SR__ s_109[] =
{
    { { DEF_RED}, {   1} }, 
    { {       0}, { -22} }, 
};

SR__ s_110[] =
{
    { { DEF_RED}, {   1} }, 
    { {       0}, { -16} }, 
};

SR__ s_111[] =
{
    { { DEF_RED}, {   1} }, 
    { {       0}, { -40} }, 
};


// State array:
SR__ *s_state[] =
{
  s_0,  s_1,  s_2,  s_3,  s_4,  s_5,  s_6,  s_7,  s_8,  s_9,
  s_10,  s_11,  s_12,  s_13,  s_14,  s_15,  s_16,  s_17,  s_18,  s_19,
  s_20,  s_21,  s_22,  s_23,  s_24,  s_25,  s_26,  s_27,  s_28,  s_29,
  s_30,  s_31,  s_32,  s_33,  s_34,  s_35,  s_36,  s_37,  s_38,  s_39,
  s_40,  s_41,  s_42,  s_43,  s_44,  s_45,  s_46,  s_47,  s_48,  s_49,
  s_50,  s_51,  s_52,  s_53,  s_54,  s_55,  s_56,  s_57,  s_58,  s_59,
  s_60,  s_61,  s_62,  s_63,  s_64,  s_65,  s_66,  s_67,  s_68,  s_69,
  s_70,  s_71,  s_72,  s_73,  s_74,  s_75,  s_76,  s_77,  s_78,  s_79,
  s_80,  s_81,  s_82,  s_83,  s_84,  s_85,  s_86,  s_87,  s_88,  s_89,
  s_90,  s_91,  s_92,  s_93,  s_94,  s_95,  s_96,  s_97,  s_98,  s_99,
  s_100,  s_101,  s_102,  s_103,  s_104,  s_105,  s_106,  s_107,  s_108,  s_109,
  s_110,  s_111,
};

} // anonymous namespace ends



// If the parsing function call uses arguments, then provide an overloaded
// function.  The code below doesn't rely on parameters, so no arguments are
// required.  Furthermore, parse uses a function try block to allow us to do
// ACCEPT and ABORT from anywhere, even from within members called by actions,
// simply throwing the appropriate exceptions.

ParserBase::ParserBase()
:
    d_stackIdx__(-1),
    // $insert debuginit
    d_debug__(false),
    d_nErrors__(0),
    // $insert requiredtokens
    d_requiredTokens__(0),
    d_acceptedTokens__(d_requiredTokens__),
    d_token__(_UNDETERMINED_),
    d_nextToken__(_UNDETERMINED_)
{}


void ParserBase::clearin()
{
    d_token__ = d_nextToken__ = _UNDETERMINED_;
}

void ParserBase::push__(size_t state)
{
    if (static_cast<size_t>(d_stackIdx__ + 1) == d_stateStack__.size())
    {
        size_t newSize = d_stackIdx__ + STACK_EXPANSION;
        d_stateStack__.resize(newSize);
        d_valueStack__.resize(newSize);
    }
    ++d_stackIdx__;
    d_stateStack__[d_stackIdx__] = d_state__ = state;
    *(d_vsp__ = &d_valueStack__[d_stackIdx__]) = d_val__;
}

void ParserBase::popToken__()
{
    d_token__ = d_nextToken__;

    d_val__ = d_nextVal__;
    d_nextVal__ = STYPE__();

    d_nextToken__ = _UNDETERMINED_;
}
     
void ParserBase::pushToken__(int token)
{
    d_nextToken__ = d_token__;
    d_nextVal__ = d_val__;
    d_token__ = token;
}
     
void ParserBase::pop__(size_t count)
{
    if (d_stackIdx__ < static_cast<int>(count))
    {
        ABORT();
    }

    d_stackIdx__ -= count;
    d_state__ = d_stateStack__[d_stackIdx__];
    d_vsp__ = &d_valueStack__[d_stackIdx__];
}

inline size_t ParserBase::top__() const
{
    return d_stateStack__[d_stackIdx__];
}

void Parser::executeAction(int production)
{
    if (d_token__ != _UNDETERMINED_)
        pushToken__(d_token__);     // save an already available token

                                    // save default non-nested block $$
    if (int size = s_productionInfo[production].d_size)
        d_val__ = d_vsp__[1 - size];

    switch (production)
    {
        // $insert actioncases
        
        case 1:
        {
         line_num++;
         d_scanner.column_num = 1;
         }
        break;

        case 2:
        {
         line_num++;
         d_scanner.column_num = 1;
         }
        break;

        case 5:
        {
         if(wasEQ) {
         error("Error: PARAMAS definition must be before EQ definition");
         exit(1);
         }
         }
        break;

        case 6:
        {
         if(testing) std::cout << "\n";
         if(wasEQ) {
         error("Error: VARS definition must be before EQ definition");
         exit(1);
         }
         }
        break;

        case 8:
        {
         if(wasEQ) {
         error("Error: CONSTS definition must be before EQ definition");
         exit(1);
         }
         }
        break;

        case 10:
        {
         if(run) {
         
         std::string ba = d_scanner.matched().substr(d_scanner.matched().find_first_not_of(" \t",3));
         
         storage.AddBaLine(ba);
         }
         }
        break;

        case 15:
        {
         if(run) {
         storage.AddVarPointsName(d_scanner.matched());
         }
         }
        break;

        case 16:
        {
         if(run) {
         storage.AddVarPointsSndValue(d_scanner.matched());
         }
         }
        break;

        case 17:
        {
         if(run) {
         storage.AddVarPointsFstValue(d_scanner.matched());
         }
         }
        break;

        case 20:
        {
         if(testing) std::cout << '\n';
         }
        break;

        case 21:
        {
         if(testing) std::cout << "Init:" << d_scanner.matched() << ":";
         if(run) {
         storage.AddInitsName(d_scanner.matched());
         }
         }
        break;

        case 22:
        {
         if(testing) std::cout << '-' << d_scanner.matched();
         if(run) {
         storage.AddInitsSndValue(d_scanner.matched());
         }
         }
        break;

        case 23:
        {
         if(testing) std::cout << d_scanner.matched();
         if(run) {
         storage.AddInitsFstValue(d_scanner.matched());
         }
         }
        break;

        case 27:
        {
         if(testing) std::cout << d_scanner.matched() << ":";
         storage.AddConstantName(d_scanner.matched());
         }
        break;

        case 28:
        {
         if(testing) std::cout << d_scanner.matched() << std::endl;
         storage.AddConstantValue(std::stod(d_scanner.matched()));
         }
        break;

        case 29:
        {
         if(testing) std::cout << "-" << d_scanner.matched() << std::endl;
         storage.AddConstantValue(std::stod(std::string("-")+d_scanner.matched()));
         }
        break;

        case 30:
        {
         if(testing) std::cout << "\n";
         }
        break;

        case 31:
        {
         if(testing) std::cout << d_scanner.matched() << ":";
         if(run) {
         storage.AddThresholdName(d_scanner.matched());
         }
         }
        break;

        case 32:
        {
         if(testing) std::cout << d_scanner.matched();
         if(run) {
         storage.AddThresholdValue(d_scanner.matched());
         }
         }
        break;

        case 33:
        {
         if(testing) std::cout << "," << d_scanner.matched();
         if(run) {
         storage.AddThresholdValue(d_scanner.matched());
         }
         }
        break;

        case 34:
        {
         if(testing) std::cout << d_scanner.matched();
         storage.AddVariable(d_scanner.matched());
         }
        break;

        case 35:
        {
         if(testing) std::cout << ',' << d_scanner.matched();
         storage.AddVariable(d_scanner.matched());
         }
        break;

        case 38:
        {
         if(testing) std::cout << "\n";
         }
        break;

        case 39:
        {
         if(testing) std::cout << ',' << d_scanner.matched();
         storage.AddParamRange(paramFirstValue,stod(d_scanner.matched()));
         }
        break;

        case 40:
        {
         if(testing) std::cout << ",-" << d_scanner.matched();
         storage.AddParamRange(paramFirstValue,std::stod(std::string("-")+d_scanner.matched()));
         }
        break;

        case 41:
        {
         if(testing) std::cout << d_scanner.matched() << ":";
         storage.AddParam(d_scanner.matched());
         }
        break;

        case 42:
        {
         if(testing) std::cout << d_scanner.matched();
         paramFirstValue = stod(d_scanner.matched());
         }
        break;

        case 43:
        {
         if(testing) std::cout << "-" << d_scanner.matched();
         paramFirstValue = std::stod(std::string("-")+d_scanner.matched());
         }
        break;

        case 44:
        {
         if(testing) std::cout << "-------END-EQ-------\n";
         if(testing || run) {
         while(!eq.empty()) {
         Entite<Parser::valueT> ent = eq.top();
         
         
         
         
         storage.AddEquation(ent.GetSummembers());
         eq.pop();
         }
         }
         }
        break;

        case 45:
        {
         wasEQ = true;
         if(testing) {
         std::cout << "------START-EQ------\n";
         std::cout << "v_eq\t" << d_scanner.matched() << std::endl;
         }
         if(run) {
         storage.AddEquationName(d_scanner.matched());
         }
         }
        break;

        case 46:
        {
         if(testing) std::cout << "c\t" << d_scanner.matched() << std::endl;
         if(run) {
         Entite<Parser::valueT> ent(storage);
         ent.PutNumber(d_scanner.matched());
         eq.push(ent);
         }
         }
        break;

        case 47:
        {
         if(testing) std::cout << "v\t" << d_scanner.matched() << std::endl;
         if(run) {
         Entite<Parser::valueT> ent(storage);
         ent.PutString(d_scanner.matched());
         eq.push(ent);
         }
         }
        break;

        case 48:
        {
         if(testing) std::cout << "r\t" << d_scanner.matched() << std::endl;
         if(run) {
         Entite<Parser::valueT> ent(storage);
         std::string matched = d_scanner.matched();
         size_t startPos = 0;
         size_t endPos = matched.find_first_of("(");
         std::string title = matched.substr(startPos,endPos);
         
         std::vector<std::string> params;
         for(int i = 0; i < 4; i++) {
         startPos = ++endPos;
         endPos = matched.find_first_of(",",startPos);
         params.push_back(matched.substr(startPos,endPos - startPos));
         
         }
         params.push_back(matched.substr(++endPos,matched.find_first_of(")",endPos) - endPos));
         
         if(title.compare(1,1,"p") == 0) {
         if(title.size() == 2) {
         ent.PutRp(params.at(0),params.at(1),params.at(2),params.at(3),params.at(4));
         } else {
         ent.PutRpCoor(params.at(0),params.at(1),params.at(2),params.at(3),params.at(4));
         }
         } else {
         if(title.size() == 2) {
         ent.PutRm(params.at(0),params.at(1),params.at(2),params.at(3),params.at(4));
         } else {
         ent.PutRmCoor(params.at(0),params.at(1),params.at(2),params.at(3),params.at(4));
         }
         }
         eq.push(ent);
         }
         }
        break;

        case 49:
        {
         if(testing) std::cout << "s\t" << d_scanner.matched() << std::endl;
         if(run) {
         Entite<Parser::valueT> ent(storage);
         std::string matched = d_scanner.matched();
         size_t startPos = 0;
         size_t endPos = matched.find_first_of("(");
         std::string title = matched.substr(startPos,endPos);
         
         std::vector<std::string> params;
         std::string points = "1";
         if(title.find("[") != std::string::npos) {
         size_t start = title.find("[");
         points = title.substr(start + 1,title.size() - 2 - start);
         
         title.erase(start);
         }
        
         for(int i = 0; i < 4; i++) {
         startPos = ++endPos;
         endPos = matched.find_first_of(",",startPos);
         params.push_back(matched.substr(startPos,endPos - startPos));
         
         }
         params.push_back(matched.substr(++endPos,matched.find_first_of(")",endPos) - endPos));
         
         if(title.compare(1,1,"p") == 0) {
         if(title.size() == 2) {
         ent.PutSp(points,params.at(0),params.at(1),params.at(2),params.at(3),params.at(4));
         } else {
         ent.PutSpInv(points,params.at(0),params.at(1),params.at(2),params.at(3),params.at(4));
         }
         } else {
         if(title.size() == 2) {
         ent.PutSm(points,params.at(0),params.at(1),params.at(2),params.at(3),params.at(4));
         } else {
         ent.PutSmInv(points,params.at(0),params.at(1),params.at(2),params.at(3),params.at(4));
         }
         }
         eq.push(ent);
         }
         }
        break;

        case 50:
        {
         if(testing) std::cout << "h\t" << d_scanner.matched() << std::endl;
         if(run) {
         Entite<Parser::valueT> ent(storage);
         std::string matched = d_scanner.matched();
         size_t startPos = 0;
         size_t endPos = matched.find_first_of("(");
         std::string title = matched.substr(startPos,endPos);
         
         std::vector<std::string> params;
         for(int i = 0; i < 3; i++) {
         startPos = ++endPos;
         endPos = matched.find_first_of(",",startPos);
         params.push_back(matched.substr(startPos,endPos - startPos));
         
         }
         params.push_back(matched.substr(++endPos,matched.find_first_of(")",endPos) - endPos));
         
         if(title.compare(1,1,"p") == 0) {
         ent.PutHp(params.at(0),params.at(1),params.at(2),params.at(3));
         } else {
         ent.PutHm(params.at(0),params.at(1),params.at(2),params.at(3));
         }
         eq.push(ent);
         }
         }
        break;

        case 51:
        {
         if(testing) std::cout << "hill\t" << d_scanner.matched() << std::endl;
         if(run) {
         Entite<Parser::valueT> ent(storage);
         std::string matched = d_scanner.matched();
         size_t startPos = 0;
         size_t endPos = matched.find_first_of("(");
         std::string title = matched.substr(startPos,endPos);
         
         std::vector<std::string> params;
         for(int i = 0; i < 4; i++) {
         startPos = ++endPos;
         endPos = matched.find_first_of(",",startPos);
         params.push_back(matched.substr(startPos,endPos - startPos));
         
         }
         params.push_back(matched.substr(++endPos,matched.find_first_of(")",endPos) - endPos));
         
         if(title.compare(4,1,"p") == 0) {
         ent.PutHillp(params.at(0),params.at(1),params.at(2),params.at(3),params.at(4));
         } else {
         ent.PutHillm(params.at(0),params.at(1),params.at(2),params.at(3),params.at(4));
         }
         eq.push(ent);
         }
         }
        break;

        case 52:
        {
         if(testing) std::cout << "c\t-" << d_scanner.matched() << std::endl;
         if(run) {
         Entite<Parser::valueT> ent(storage);
         ent.PutNumber(string(d_scanner.matched()));
         ent.negate();
         eq.push(ent);
         }
         }
        break;

        case 53:
        {
         if(testing) std::cout << "v\t-" << d_scanner.matched() << std::endl;
         if(run) {
         Entite<Parser::valueT> ent(storage);
         ent.PutString(d_scanner.matched());
         ent.negate();
         eq.push(ent);
         }
         }
        break;

        case 54:
        {
         if(testing) std::cout << "r\t-" << d_scanner.matched() << std::endl;
         if(run) {
         Entite<Parser::valueT> ent(storage);
         std::string matched = d_scanner.matched();
         size_t startPos = 0;
         size_t endPos = matched.find_first_of("(");
         std::string title = matched.substr(startPos,endPos);
         
         std::vector<std::string> params;
         for(int i = 0; i < 4; i++) {
         startPos = ++endPos;
         endPos = matched.find_first_of(",",startPos);
         params.push_back(matched.substr(startPos,endPos - startPos));
         
         }
         params.push_back(matched.substr(++endPos,matched.find_first_of(")",endPos) - endPos));
         
         if(title.compare(1,1,"p") == 0) {
         if(title.size() == 2) {
         ent.PutRp(params.at(0),params.at(1),params.at(2),params.at(3),params.at(4));
         } else {
         ent.PutRpCoor(params.at(0),params.at(1),params.at(2),params.at(3),params.at(4));
         }
         } else {
         if(title.size() == 2) {
         ent.PutRm(params.at(0),params.at(1),params.at(2),params.at(3),params.at(4));
         } else {
         ent.PutRmCoor(params.at(0),params.at(1),params.at(2),params.at(3),params.at(4));
         }
         }
         ent.negate();
         eq.push(ent);
         }
         }
        break;

        case 55:
        {
         if(testing) std::cout << "s\t-" << d_scanner.matched() << std::endl;
         if(run) {
         Entite<Parser::valueT> ent(storage);
         std::string matched = d_scanner.matched();
         size_t startPos = 0;
         size_t endPos = matched.find_first_of("(");
         std::string title = matched.substr(startPos,endPos);
         
         std::vector<std::string> params;
         std::string points = "1";
         if(title.find("[") != std::string::npos) {
         size_t start = title.find("[");
         points = title.substr(start + 1,title.size() - 2 - start);
         
         title.erase(start);
         }
        
         for(int i = 0; i < 4; i++) {
         startPos = ++endPos;
         endPos = matched.find_first_of(",",startPos);
         params.push_back(matched.substr(startPos,endPos - startPos));
         
         }
         params.push_back(matched.substr(++endPos,matched.find_first_of(")",endPos) - endPos));
         
         if(title.compare(1,1,"p") == 0) {
         if(title.size() == 2) {
         ent.PutSp(points,params.at(0),params.at(1),params.at(2),params.at(3),params.at(4));
         } else {
         ent.PutSpInv(points,params.at(0),params.at(1),params.at(2),params.at(3),params.at(4));
         }
         } else {
         if(title.size() == 2) {
         ent.PutSm(points,params.at(0),params.at(1),params.at(2),params.at(3),params.at(4));
         } else {
         ent.PutSmInv(points,params.at(0),params.at(1),params.at(2),params.at(3),params.at(4));
         }
         }
         ent.negate();
         eq.push(ent);
         }
         }
        break;

        case 56:
        {
         if(testing) std::cout << "h\t-" << d_scanner.matched() << std::endl;
         if(run) {
         Entite<Parser::valueT> ent(storage);
         std::string matched = d_scanner.matched();
         size_t startPos = 0;
         size_t endPos = matched.find_first_of("(");
         std::string title = matched.substr(startPos,endPos);
         
         std::vector<std::string> params;
         for(int i = 0; i < 3; i++) {
         startPos = ++endPos;
         endPos = matched.find_first_of(",",startPos);
         params.push_back(matched.substr(startPos,endPos - startPos));
         
         }
         params.push_back(matched.substr(++endPos,matched.find_first_of(")",endPos) - endPos));
         
         if(title.compare(1,1,"p") == 0) {
         ent.PutHp(params.at(0),params.at(1),params.at(2),params.at(3));
         } else {
         ent.PutHm(params.at(0),params.at(1),params.at(2),params.at(3));
         }
         ent.negate();
         eq.push(ent);
         }
         }
        break;

        case 57:
        {
         if(testing) std::cout << "hill\t-" << d_scanner.matched() << std::endl;
         if(run) {
         Entite<Parser::valueT> ent(storage);
         std::string matched = d_scanner.matched();
         size_t startPos = 0;
         size_t endPos = matched.find_first_of("(");
         std::string title = matched.substr(startPos,endPos);
         
         std::vector<std::string> params;
         for(int i = 0; i < 4; i++) {
         startPos = ++endPos;
         endPos = matched.find_first_of(",",startPos);
         params.push_back(matched.substr(startPos,endPos - startPos));
         
         }
         params.push_back(matched.substr(++endPos,matched.find_first_of(")",endPos) - endPos));
         
         if(title.compare(4,1,"p") == 0) {
         ent.PutHillp(params.at(0),params.at(1),params.at(2),params.at(3),params.at(4));
         } else {
         ent.PutHillm(params.at(0),params.at(1),params.at(2),params.at(3),params.at(4));
         }
         ent.negate();
         eq.push(ent);
         }
         }
        break;

        case 58:
        {
         if(verbatim) std::cout << "expr -> summation\n";
         }
        break;

        case 59:
        {
         if(verbatim) std::cout << "expr -> ap\n";
         }
        break;

        case 60:
        {
         if(verbatim) std::cout << "summation -> summant\n";
         }
        break;

        case 61:
        {
         if(verbatim) std::cout << "summation -> summation + summant\n";
         if(run) {
         Entite<Parser::valueT> temp = eq.top();
         eq.pop();
         Entite<Parser::valueT> ent = temp + eq.top();
         eq.pop();
         eq.push(ent);
         }
         }
        break;

        case 62:
        {
         if(verbatim) std::cout << "summation -> summation - summant\n";
         if(run) {
         Entite<Parser::valueT> temp = eq.top();
         eq.pop();
         temp.negate();
         Entite<Parser::valueT> ent = eq.top() + temp;
         eq.pop();
         eq.push(ent);
         }
         }
        break;

        case 63:
        {
         if(verbatim) std::cout << "summant -> multiplication\n";
         }
        break;

        case 64:
        {
         if(verbatim) std::cout << "summant -> parenthesis\n";
         }
        break;

        case 65:
        {
         if(verbatim) std::cout << "multiplication -> multiple\n";
         }
        break;

        case 66:
        {
         if(verbatim) std::cout << "multiplication -> multiplication * multiple\n";
         if(run) {
         Entite<Parser::valueT> temp = eq.top();
         eq.pop();
         Entite<Parser::valueT> ent = temp * eq.top();
         eq.pop();
         eq.push(ent);
         }
         }
        break;

        case 67:
        {
         if(verbatim) std::cout << "multiple -> ap\n";
         }
        break;

        case 68:
        {
         if(verbatim) std::cout << "multiple -> parenthesis\n";
         }
        break;

        case 69:
        {
         if(verbatim) std::cout << "parenthesis -> ( expr )\n";
         }
        break;

    }
}

inline void ParserBase::reduce__(PI__ const &pi)
{
    d_token__ = pi.d_nonTerm;
    pop__(pi.d_size);

}

// If d_token__ is _UNDETERMINED_ then if d_nextToken__ is _UNDETERMINED_ another
// token is obtained from lex(). Then d_nextToken__ is assigned to d_token__.
void Parser::nextToken()
{
    if (d_token__ != _UNDETERMINED_)        // no need for a token: got one
        return;                             // already

    if (d_nextToken__ != _UNDETERMINED_)
    {
        popToken__();                       // consume pending token
    }
    else
    {
        ++d_acceptedTokens__;               // accept another token (see
                                            // errorRecover())
        d_token__ = lex();
        if (d_token__ <= 0)
            d_token__ = _EOF_;
    }
    print();
}

// if the final transition is negative, then we should reduce by the rule
// given by its positive value. Note that the `recovery' parameter is only
// used with the --debug option
int Parser::lookup(bool recovery)
{
    // $insert threading
    SR__ *sr = s_state[d_state__];        // get the appropriate state-table
    int lastIdx = sr->d_lastIdx;        // sentinel-index in the SR__ array

    SR__ *lastElementPtr = sr + lastIdx;
    SR__ *elementPtr = sr + 1;            // start the search at s_xx[1]

    lastElementPtr->d_token = d_token__;// set search-token

    while (elementPtr->d_token != d_token__)
        ++elementPtr;

    if (elementPtr == lastElementPtr)   // reached the last element
    {
        if (elementPtr->d_action < 0)   // default reduction
        {
            return elementPtr->d_action;                
        }

        // No default reduction, so token not found, so error.
        throw UNEXPECTED_TOKEN__;
    }

    // not at the last element: inspect the nature of the action
    // (< 0: reduce, 0: ACCEPT, > 0: shift)

    int action = elementPtr->d_action;


    return action;
}

    // When an error has occurred, pop elements off the stack until the top
    // state has an error-item. If none is found, the default recovery
    // mode (which is to abort) is activated. 
    //
    // If EOF is encountered without being appropriate for the current state,
    // then the error recovery will fall back to the default recovery mode.
    // (i.e., parsing terminates)
void Parser::errorRecovery()
try
{
    if (d_acceptedTokens__ >= d_requiredTokens__)// only generate an error-
    {                                           // message if enough tokens 
        ++d_nErrors__;                          // were accepted. Otherwise
        error("Syntax error");                  // simply skip input

    }


    // get the error state
    while (not (s_state[top__()][0].d_type & ERR_ITEM))
    {
        pop__();
    }

    // In the error state, lookup a token allowing us to proceed.
    // Continuation may be possible following multiple reductions,
    // but eventuall a shift will be used, requiring the retrieval of
    // a terminal token. If a retrieved token doesn't match, the catch below 
    // will ensure the next token is requested in the while(true) block
    // implemented below:

    int lastToken = d_token__;                  // give the unexpected token a
                                                // chance to be processed
                                                // again.

    pushToken__(_error_);                       // specify _error_ as next token
    push__(lookup(true));                       // push the error state

    d_token__ = lastToken;                      // reactivate the unexpected
                                                // token (we're now in an
                                                // ERROR state).

    bool gotToken = true;                       // the next token is a terminal

    while (true)
    {
        try
        {
            if (s_state[d_state__]->d_type & REQ_TOKEN)
            {
                gotToken = d_token__ == _UNDETERMINED_;
                nextToken();                    // obtain next token
            }
            
            int action = lookup(true);

            if (action > 0)                 // push a new state
            {
                push__(action);
                popToken__();

                if (gotToken)
                {

                    d_acceptedTokens__ = 0;
                    return;
                }
            }
            else if (action < 0)
            {
                // no actions executed on recovery but save an already 
                // available token:
                if (d_token__ != _UNDETERMINED_)
                    pushToken__(d_token__);
 
                                            // next token is the rule's LHS
                reduce__(s_productionInfo[-action]); 
            }
            else
                ABORT();                    // abort when accepting during
                                            // error recovery
        }
        catch (...)
        {
            if (d_token__ == _EOF_)
                ABORT();                    // saw inappropriate _EOF_
                      
            popToken__();                   // failing token now skipped
        }
    }
}
catch (ErrorRecovery__)       // This is: DEFAULT_RECOVERY_MODE
{
    ABORT();
}

    // The parsing algorithm:
    // Initially, state 0 is pushed on the stack, and d_token__ as well as
    // d_nextToken__ are initialized to _UNDETERMINED_. 
    //
    // Then, in an eternal loop:
    //
    //  1. If a state does not have REQ_TOKEN no token is assigned to
    //     d_token__. If the state has REQ_TOKEN, nextToken() is called to
    //      determine d_nextToken__ and d_token__ is set to
    //     d_nextToken__. nextToken() will not call lex() unless d_nextToken__ is 
    //     _UNDETERMINED_. 
    //
    //  2. lookup() is called: 
    //     d_token__ is stored in the final element's d_token field of the
    //     state's SR_ array. 
    //
    //  3. The current token is looked up in the state's SR_ array
    //
    //  4. Depending on the result of the lookup() function the next state is
    //     shifted on the parser's stack, a reduction by some rule is applied,
    //     or the parsing function returns ACCEPT(). When a reduction is
    //     called for, any action that may have been defined for that
    //     reduction is executed.
    //
    //  5. An error occurs if d_token__ is not found, and the state has no
    //     default reduction. Error handling was described at the top of this
    //     file.

int Parser::parse()
try 
{
    push__(0);                              // initial state
    clearin();                              // clear the tokens.

    while (true)
    {
        try
        {
            if (s_state[d_state__]->d_type & REQ_TOKEN)
                nextToken();                // obtain next token


            int action = lookup(false);     // lookup d_token__ in d_state__

            if (action > 0)                 // SHIFT: push a new state
            {
                push__(action);
                popToken__();               // token processed
            }
            else if (action < 0)            // REDUCE: execute and pop.
            {
                executeAction(-action);
                                            // next token is the rule's LHS
                reduce__(s_productionInfo[-action]); 
            }
            else 
                ACCEPT();
        }
        catch (ErrorRecovery__)
        {
            errorRecovery();
        }
    }
}
catch (Return__ retValue)
{
    return retValue;
}

